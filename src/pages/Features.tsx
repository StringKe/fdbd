import { Box } from '@chakra-ui/react'
import React from 'react'

import wrapLayout from '../layout'

const Features = wrapLayout(() => {
    return <Box>Features</Box>
}, 'default')

export default Features
