import { Box } from '@chakra-ui/react'
import React from 'react'

import wrapLayout from '../layout'

const NotFound = wrapLayout(() => {
    return <Box>NotFound</Box>
}, 'clean')

export default NotFound
